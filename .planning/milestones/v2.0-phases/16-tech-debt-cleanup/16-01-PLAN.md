---
phase: 16-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/13-pre-existing-test-fixes/13-01-PLAN.md
  - .planning/phases/13-pre-existing-test-fixes/13-01-SUMMARY.md
  - frontend/lib/widgets/vlvt_button.dart
autonomous: true
requirements: []
must_haves:
  truths:
    - "ROADMAP.md Phase 15 progress row has all 5 columns correctly aligned (Phase | v2.0 | 1/1 | Complete | date)"
    - "ROADMAP.md Phase 15 plan checkbox shows [x] not [ ]"
    - "ROADMAP.md Phase 16 progress row shows 1/1 plans and Complete status"
    - "VlvtIconButton class no longer exists in vlvt_button.dart"
    - "VlvtButton class in vlvt_button.dart is completely untouched"
    - "No references to TECHDEBT-13 exist in plan or summary frontmatter"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected progress table and plan checkboxes"
    - path: "frontend/lib/widgets/vlvt_button.dart"
      provides: "VlvtButton only (VlvtIconButton dead code removed)"
  key_links: []
---

<objective>
Fix three tech debt items from the v2.0 milestone audit: correct ROADMAP.md progress table misalignment and stale checkbox, remove orphaned VlvtIconButton dead code, and clean up informal TECHDEBT-13 requirement ID references.

Purpose: Close out the final v2.0 milestone phase by resolving accumulated documentation inconsistencies and dead code.
Output: Clean ROADMAP.md, leaner vlvt_button.dart, consistent frontmatter across Phase 13 artifacts.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tech-debt-cleanup/16-RESEARCH.md
@frontend/lib/widgets/vlvt_button.dart
@.planning/phases/13-pre-existing-test-fixes/13-01-PLAN.md
@.planning/phases/13-pre-existing-test-fixes/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md progress table and plan checkboxes</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Make three edits to ROADMAP.md:

**Edit A — Fix Phase 15 progress row (line 172):**
The Phase 15 row is missing the `v2.0` milestone value, shifting all columns left. Replace:
```
| 15. Chat-Service Shutdown Ordering | 1/1 | Complete    | 2026-02-28 | — |
```
With:
```
| 15. Chat-Service Shutdown Ordering | v2.0 | 1/1 | Complete | 2026-02-28 |
```

**Edit B — Fix Phase 15 plan checkbox (line 145):**
Phase 15 is complete but its plan checkbox still shows unchecked. Replace:
```
- [ ] 15-01-PLAN.md — Wrap io.close() in Promise and await it before pool.end() (RESIL-06)
```
With:
```
- [x] 15-01-PLAN.md — Wrap io.close() in Promise and await it before pool.end() (RESIL-06)
```

**Edit C — Update Phase 16 plan count and add plan list (around lines 156-157):**
Replace:
```
**Plans**: TBD
```
With:
```
**Plans**: 1 plan

Plans:
- [ ] 16-01-PLAN.md — Fix ROADMAP alignment, remove orphaned VlvtIconButton, clean TECHDEBT-13 references
```

**Edit D — Update Phase 16 progress row (line 173):**
Replace:
```
| 16. Tech Debt Cleanup | v2.0 | 0/0 | Planned | — |
```
With:
```
| 16. Tech Debt Cleanup | v2.0 | 0/1 | In Progress | — |
```

**Edit E — Update success criterion 1 (line 153):**
The success criterion references Phase 14, but the research confirmed Phase 14's row is already correct. The actual issue is Phase 15's row. Replace:
```
  1. ROADMAP.md Phase 14 progress row shows correct `2/2` in Plans Complete column
```
With:
```
  1. ROADMAP.md Phase 15 progress row has correct column alignment with `v2.0` milestone value
```

Do NOT modify Phase 14's progress row -- it is already correct at `| 14. Documentation Cleanup | v2.0 | 2/2 | Complete | 2026-02-28 |`.
  </action>
  <verify>
    <automated>cd "C:/Users/dasbl/AndroidStudioProjects/VLVT" && grep "| 15. Chat-Service Shutdown Ordering | v2.0 | 1/1 |" .planning/ROADMAP.md && grep "\[x\] 15-01-PLAN.md" .planning/ROADMAP.md && grep "| 16. Tech Debt Cleanup | v2.0 | 0/1 |" .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 15 progress row has all 5 columns correctly aligned with v2.0 milestone. Phase 15 plan checkbox shows [x]. Phase 16 row shows 0/1 plans in progress. Success criterion 1 accurately describes the Phase 15 row fix.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned VlvtIconButton and clean TECHDEBT-13 references</name>
  <files>frontend/lib/widgets/vlvt_button.dart, .planning/phases/13-pre-existing-test-fixes/13-01-PLAN.md, .planning/phases/13-pre-existing-test-fixes/13-01-SUMMARY.md</files>
  <action>
**Edit A — Remove VlvtIconButton from vlvt_button.dart:**
Remove lines 304-364 from `frontend/lib/widgets/vlvt_button.dart`. This includes the blank line after VlvtButton's closing brace (line 304), the doc comment `/// An icon-only button for the VLVT design system.` (line 305), and the entire `VlvtIconButton` class (lines 306-364).

The file should end at line 303 (the closing brace of `_VlvtButtonState.build()`). Do NOT modify anything in lines 1-303 -- VlvtButton must remain completely untouched.

**Verification:** VlvtIconButton has zero callers in the codebase (confirmed by grep). Removing it has zero runtime impact.

**Edit B — Clean TECHDEBT-13 from 13-01-PLAN.md frontmatter (line 9):**
Replace:
```yaml
requirements: ["TECHDEBT-13"]
```
With:
```yaml
requirements: []
```

**Edit C — Clean TECHDEBT-13 from 13-01-SUMMARY.md frontmatter (line 30):**
Replace:
```yaml
requirements-completed: [TECHDEBT-13]
```
With:
```yaml
requirements-completed: []
```

Do NOT modify `14-RESEARCH.md` -- its TECHDEBT-13 reference is informational context about what existed at research time, not a requirement reference.
  </action>
  <verify>
    <automated>cd "C:/Users/dasbl/AndroidStudioProjects/VLVT" && grep -c "VlvtIconButton" frontend/lib/widgets/vlvt_button.dart | grep "^0$" && grep -c "VlvtButton" frontend/lib/widgets/vlvt_button.dart | grep -v "^0$" && grep "requirements:" .planning/phases/13-pre-existing-test-fixes/13-01-PLAN.md | grep "\[\]" && grep "requirements-completed:" .planning/phases/13-pre-existing-test-fixes/13-01-SUMMARY.md | grep "\[\]" && cd frontend && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <done>VlvtIconButton class removed from vlvt_button.dart. VlvtButton class untouched and still present. TECHDEBT-13 removed from both 13-01-PLAN.md and 13-01-SUMMARY.md frontmatter. flutter analyze passes with no new errors.</done>
</task>

</tasks>

<verification>
1. `grep "| 15. Chat-Service Shutdown Ordering | v2.0 | 1/1 | Complete | 2026-02-28 |" .planning/ROADMAP.md` — Phase 15 row aligned
2. `grep "\[x\] 15-01-PLAN.md" .planning/ROADMAP.md` — Phase 15 plan checked
3. `grep "VlvtIconButton" frontend/lib/widgets/vlvt_button.dart` — returns nothing (class removed)
4. `grep "class VlvtButton" frontend/lib/widgets/vlvt_button.dart` — returns match (class preserved)
5. `grep "TECHDEBT-13" .planning/phases/13-pre-existing-test-fixes/13-01-PLAN.md .planning/phases/13-pre-existing-test-fixes/13-01-SUMMARY.md` — returns nothing
6. `cd frontend && flutter analyze --no-fatal-infos` — no new errors
</verification>

<success_criteria>
1. ROADMAP.md Phase 15 progress row shows `| 15. Chat-Service Shutdown Ordering | v2.0 | 1/1 | Complete | 2026-02-28 |` with correct 5-column alignment
2. ROADMAP.md Phase 15 plan checkbox shows `[x]`
3. ROADMAP.md Phase 16 has plan list and updated progress row
4. VlvtIconButton class is completely removed from vlvt_button.dart
5. VlvtButton class remains intact in vlvt_button.dart (zero changes to lines 1-303)
6. TECHDEBT-13 no longer appears in 13-01-PLAN.md or 13-01-SUMMARY.md frontmatter
7. `flutter analyze` passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-tech-debt-cleanup/16-01-SUMMARY.md`
</output>
